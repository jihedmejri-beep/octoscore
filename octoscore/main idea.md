Please make these specific fixes to the current UI:

1. LOGO: Remove the emoji placeholder. In the header, display the 
   OctoScore text logo only for now — styled with "OCTO" in white 
   and "SCORE" in #7C3AED purple, bold uppercase, large font. 
   We will add the real logo image later.

2. REMOVE ALL EMOJIS everywhere in the app — navbar icons, 
   team badges, scorer list, everywhere. Replace with either:
   - Clean text initials in a colored circle for team badges
   - Simple SVG icons for the navbar (home, trophy, calendar, 
     users, brain — use heroicons style, outline only)
   - No decorative emojis anywhere

3. Keep everything else exactly as is — layout, colors, 
   structure, navbar pill style. Only fix the two issues above.